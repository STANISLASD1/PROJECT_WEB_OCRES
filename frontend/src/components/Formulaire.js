import * as React from 'react';

export default function Formulaire() {

    return (
        <div className="container">
            <h2>Selectionner la tache à effectuer :</h2>
            <button>Ajouter</button>
            <button>Supprimer</button>
        </div>
    );
}